# requirements.txt for SparkVy FastAPI + MongoDB backend (main.py)
# Generated by installing each direct dependency into a clean, isolated
# virtual environment and verifying `main.py` imports and runs against it.
#
# Direct (imported) dependencies
fastapi==0.140.1
pydantic==2.13.4
pymongo==4.17.0
certifi==2025.1.31

# Required to actually serve the app in production (`uvicorn main:app`).
# Not imported inside main.py, but mandatory for deployment.
uvicorn==0.51.0
